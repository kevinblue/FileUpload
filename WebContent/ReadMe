1.进行文件上传，
1）请求方式为post:
2)使用file的表单域：<input type="file" name="file"/>
3)使用新的编码方式： multipart/form-data  表示表单以二进制的方式传送
  <form action="uploadServlet"  method="post" enctype="multipart/form-data">
  例子：
  <form action="uploadServlet"  method="post" enctype="multipart/form-data">
        File:<input type="file" name="file"/>
        <input type="submit" value="submit"/> 
     
   </form>
   
   4）关于enctype:
   >application/x-wwww-form-urlencoded:表单enctype属性的默认值。这种编码方案使用有线的字符集，当使用非字母和数字时，必须使用
   " %HH"代替（H 代表十六进制数字）。对于大容量的二进制数据或者包含非ASCII字符的文本来说。这种编码不能满足要求
   > multipart/form-data  表示表单以二进制的方式传送
   5)服务端：
   1)不能使用request.getParameter()等方式获取请求信息，因为请求的编码方式已经改为multipart/form-data，以 二进制的方式
   提交请求信息
   
   
   
   抽取方法的
   
   refacter --extact method private
   
   
   ==================
   问题：
   每次获取都得到的是工作路径下的.metadata下的项目路径？如以下路径：“E:\DropBox\Dropbox\workspace\.metadata\.plugins\org.eclipse.wst.server.core\tmp0\wtpwebapps\reportManage\WEB-INF”。希望各位大神指示下。！
感谢各位大神们的帮助，我也明白了一些。发现，我的ide是eclipse，服务器是tomcat 6 ，每次启动tomcat时。我的项目都会发布到： \.metadata\.plugins\org.eclipse.wst.server.core\tmp2\wtpwebapps\reportManage\WEB-INF 。这个目录下。但这个目录似乎是临时目录。
网上查了下资料，说是可能是用了tomcat插件，的确我这个eclipse用了tomcat插件，但是并未使用插件来启动项目啊？
还希望大神们能继续帮助！
   
   解答：
   这个请求路径不是你的源码路径，而是服务器加载后生成的，你可以去服务器文件夹下看看